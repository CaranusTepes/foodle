<div>
  <% @categoryList.each do |category| %>
    <div>
      <%= category["strCategory"] %>
      <%= category["strCategoryDescription"] %>
      <%= image_tag category["strCategoryThumb"]%>
    </div>
  <% end %>
</div>